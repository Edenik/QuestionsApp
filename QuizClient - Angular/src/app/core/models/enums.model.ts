export enum Difficulity {
  easy = 'easy',
  medium = 'medium',
  hard = 'hard',
}

export enum EditQuestionMode {
  create,
  edit,
}

export enum GameStatus {
  awiating = 'awaiting',
  start = 'start',
  end = 'end',
}
